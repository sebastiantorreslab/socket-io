module.exports = {
    preset: 'ts-jest', // Usar ts-jest para TypeScript
    testEnvironment: 'node', // Entorno de pruebas (Node.js)
    testMatch: ['**/*.test.ts'], // Buscar archivos de prueba
  };